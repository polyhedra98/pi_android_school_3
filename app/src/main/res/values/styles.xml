<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CategoryHeadline" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="RemoveButton" parent="Widget.MaterialComponents.Button">
        <item name="android:drawableLeft">@drawable/ic_delete_24px</item>
    </style>

    <style name="SearchButton" parent="Widget.MaterialComponents.Button">
        <item name="android:drawableLeft">@drawable/ic_search_24px</item>
    </style>

</resources>
